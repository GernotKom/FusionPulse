{
  "name": "fusionpulse",
  "version": "2.5.5a",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-version": "node scripts/sync-version.mjs",
    "predev": "npm run sync-version",
    "dev": "wrangler dev",
    "predeploy": "npm run sync-version",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "node --check public/app.js && node --check src/worker.js && node --check public/sw.js"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
