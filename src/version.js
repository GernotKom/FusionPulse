/* ============================================================================
   EINZIGE VERSIONSQUELLE — DIESE DATEI WIRD GENERIERT.
   Nicht von Hand editieren. Quelle ist "version" in package.json.
   Aktualisieren mit:  npm run sync-version
   Der Sync schreibt dieselbe Nummer nach:
     src/version.js · public/version.js · public/sw.js
     public/index.html (<title>) · wrangler.jsonc (vars.APP_VERSION)
   ========================================================================== */
export const APP_VERSION = '4.2.7';
export default APP_VERSION;
