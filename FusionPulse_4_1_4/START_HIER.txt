FusionPulse v2.5.0 – Update

GH (GitHub):
- Dateien/Ordner direkt ins Repository-Root hochladen: public/, src/, package.json, wrangler.jsonc, README.md usw.
- Keine zusätzliche FusionPulse-Unterebene.

CF (Cloudflare):
- Build-Stammverzeichnis: /
- Bestehende FUSION_API_KEY und APP_TOKEN nicht verändern.
- Für Aktienradar zusätzlich TWELVE_API_KEY als Geheimnis anlegen.
- Nach Secret-Änderung ggf. unter Bereitstellungen die neueste Version auf 100 % befördern.

PWA:
- neu laden; Version v2.5.0 prüfen.
- Aktienradar braucht im Free-Modus bis ca. 15 Minuten, bis alle 21 Titel einmal aktualisiert wurden.
