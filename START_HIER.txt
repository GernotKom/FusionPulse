FusionPulse v5.0.0 – Sparbetrieb auf Workers Free

GH: Dateien/Ordner direkt ins Repository-Root. CF: Build-Stammverzeichnis /.
Keine Secrets, keine Migration, kein Binding. Code-Logik unveraendert.

REIHENFOLGE: erst im Cloudflare-Dashboard auf Workers Free zurueckstufen,
dann deployen.

1. wrangler.jsonc
   - Block "limits" entfernt (Free lehnt ihn ab, Fehler 100328)
   - CF_PLAN "free", D1_WRITE_BUDGET "90000"
   - Cron statt jeder Minute: "3 18,19 * * 1-5" (werktags 18:03/19:03 UTC).
     Einer der beiden Laeufe trifft ganzjaehrig 20:03 Wien = Tageslauf.

2. FOLGE
   - Tagesempfehlung um 20:00 Wien bleibt.
   - Krypto-Zyklus und laufende Watchlist-Scans ruhen.
   - Die Lernschicht sammelt praktisch nicht mehr (LEARN_MIN_OBS = 6
     Beobachtungen je Fenster werden bei 2 Laeufen/Tag nicht erreicht).
   - Daten werden sonst nur geholt, wenn die App geoeffnet ist.

3. ZURUECK ZUM DAUERBETRIEB (Paid)
   "limits": { "cpu_ms": 5000, "subrequests": 1000 }, CF_PLAN "paid",
   D1_WRITE_BUDGET "1000000", Cron ["* * * * *"].

Nach dem ersten Tageslauf im Log pruefen, ob er unter 10 ms CPU (Free) bleibt.
